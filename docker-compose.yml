version: '3.8'
services:
  web:
    build: .
    container_name: chatbot-websearch
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
