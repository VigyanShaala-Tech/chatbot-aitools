version: '3.8'
services:
  web:
    build: .
    container_name: chatbot-websearch
    ports:
      - "3001:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
