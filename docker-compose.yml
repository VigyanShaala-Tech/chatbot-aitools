version: '3.8'
services:
  web:
    build: .
    container_name: chatbot-websearch
    ports:
      - "3001:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs

